/** Google Drive backup adapter. Supply an OAuth access token from Google Identity Services. */
export async function uploadBackup(accessToken, archive){const body=new FormData();body.append('metadata',new Blob([JSON.stringify({name:'GlassNotes_Backup.zip',mimeType:'application/zip'})],{type:'application/json'}));body.append('file',archive);const response=await fetch('https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart',{method:'POST',headers:{Authorization:`Bearer ${accessToken}`},body});if(!response.ok)throw new Error('Drive backup failed');return response.json();}
